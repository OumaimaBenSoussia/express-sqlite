version: '3'
services:
  app:
    image: oumaimabensoussia/express-sqlite-app
    container_name: express-sqlite-app-1
    volumes:
      - ./mydatabase.db:/usr/src/app/mydatabase.db
    ports:
      - "3000:3000"